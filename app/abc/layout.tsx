const AbcLayout = ({children}: {children: React.ReactNode}) => {
    return (
        <div>
            <h1>
                Hello from abc layout
            </h1>
            {children}
        </div>
    )
}

export default AbcLayout